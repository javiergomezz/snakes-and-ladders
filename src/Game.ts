import { Board } from "./Board";
import { Token } from "./Token";

export class Game {
    start(): void { }
    
    moveToken(spaces: number): void { }
    
    getTokenPosition(): number {
        return  1;
    }
}